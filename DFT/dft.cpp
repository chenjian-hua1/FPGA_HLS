#include "dft.h"
#include <iostream>

void dft(DTYPE real_samples[SIZE], DTYPE imag_samples[SIZE], SUM_DTYPE real_outs[SIZE], SUM_DTYPE imag_outs[SIZE]) {
    sin
}

int main() {
    DTYPE real_samples[SIZE]; 
    DTYPE imag_samples[SIZE]; 
    SUM_DTYPE real_outs[SIZE];
    SUM_DTYPE imag_outs[SIZE];

    dft(real_samples, imag_samples, real_outs, imag_outs);
    std::cout << "success" << std::endl;
    return 0;
}
